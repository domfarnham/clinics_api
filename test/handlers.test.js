const expect = require('chai').expect
const request = require('supertest')
const findWithPostCode = require('../lib/clinics').findWithPostCode

describe('clinics', () => {
  describe('findWithPostCode', function () {
    it('should return JSON', function () {
      expect()
    })
  })
})
